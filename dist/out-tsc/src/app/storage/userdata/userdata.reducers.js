// Section 1
var initialState = {
    name: 'Initial Tutorial',
    url: 'http://google.com',
    points: 100
};
// Section 2
export function reducer(state, action) {
    if (state === void 0) { state = [initialState]; }
    // Section 3
    switch (action.type) {
        /*
        case UserDataActions.ADD_MONEY:
            return [...state, action.payload];
        case UserDataActions.ADD_MONEY:
            return [...state, action.payload];
        default:
            return state;
        */
    }
}
//# sourceMappingURL=userdata.reducers.js.map