import { TestBed } from '@angular/core/testing';
import { StoreToFirebaseService } from './store-to-firebase.service';
describe('StoreToFirebaseService', function () {
    beforeEach(function () { return TestBed.configureTestingModule({}); });
    it('should be created', function () {
        var service = TestBed.get(StoreToFirebaseService);
        expect(service).toBeTruthy();
    });
});
//# sourceMappingURL=store-to-firebase.service.spec.js.map