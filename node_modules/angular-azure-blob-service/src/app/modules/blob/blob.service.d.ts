import { HttpClient } from '@angular/common/http';
import { UploadParams, UploadConfig } from './definitions';
export declare class BlobService {
    private http;
    static DefaultBlockSize: number;
    constructor(http: HttpClient);
    generateBlobUrl(params: UploadParams, filename: string, useAzureStorageEmulator?: boolean, azureStorageEmulatorBaseUrl?: string): string;
    private uploadFileInBlocks;
    private commitBlockList;
    private initializeState;
    upload(config: UploadConfig): {
        cancel: () => void;
    };
    private prependZeros;
}
